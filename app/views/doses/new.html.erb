<h4>Add The Ingredients:</h4>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association :ingredient %>
  <%= f.input :description %>
  <%= f.button :submit, "Add It!", class: "btn btn-primary" %>
<% end %>

<%= link_to "Go Back To List", cocktails_path %>
